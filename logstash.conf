input {
  file {
    path => "/var/log/dpkg.log"
    start_position => "beginning"
  }
}

output {
  elasticsearch {
    hosts => ["192.168.30.6:9200"]
    index => "ir-logstash-%{+YYYY.MM.dd}"
  }
}
